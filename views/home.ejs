<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= titulo %></title>
  <link rel="stylesheet" href="/home1.css">
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
</head>

<body class="<%= tema === 'escuro' ? 'dark-mode' : '' %>">
  <header class="navbar">
    <div class="menu-toggle" onclick="toggleMenu()">☰</div>
    <h1>FutureFest Noesis</h1>
    <nav id="menu">
      <a href="/home">Início</a>
      <a href="/curso2">Cursos</a>
      <a href="/explore2">Explorar</a>
      <a href="/opt2">Oportunidades</a>
      <a href="/help2">Ajuda</a>
      <a href="/sair" class="logout">Sair</a>
    </nav>
  </header>

  <main class="conteudo">
    <section class="perfil">
      <img src="<%= usuario.foto || '/img/user-default.png' %>" alt="Foto de perfil" class="foto-perfil">
      <div class="info-usuario">
        <h2>Olá, <%= usuario.usuario %> 👋</h2>
        <p>Bem-vindo de volta à plataforma!</p>
      </div>
    </section>

    <section class="progresso">
      <h3>📚 Seus Cursos</h3>
      <% if (usuario.cursosEstudados && usuario.cursosEstudados.length > 0) { %>
        <ul class="lista-cursos">
          <% usuario.cursosEstudados.forEach(curso => { %>
            <li>
              <strong><%= curso.nome %></strong><br>
              Duração: <%= curso.duracao %><br>
              Progresso: <%= curso.progresso %>%
              <div class="barra-progresso">
                <div class="preenchimento" style="width: <%= curso.progresso %>%"></div>
              </div>
            </li>
          <% }) %>
        </ul>
      <% } else { %>
        <p>Você ainda não iniciou nenhum curso. Explore <a href="/curso2">novos cursos</a>!</p>
      <% } %>
    </section>
  </main>

  <script>
    function toggleMenu() {
      document.getElementById('menu').classList.toggle('ativo');
    }
  </script>
</body>
</html>
